# Pick ONE provider for M0. The provider field in config.json decides which is used.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional: where local data lives. Defaults to ~/Library/Application Support/newsline
# NEWSLINE_DATA_DIR=
